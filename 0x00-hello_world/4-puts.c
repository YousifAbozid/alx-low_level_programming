#include <stdio.h>

/*
* This is the main function works as entry point
* and always returns 0 for successful exit
*/

int main(void)
{
  puts("\"Programming is like building a multilingual puzzle");
  return(0);
}