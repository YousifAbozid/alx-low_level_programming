#include "main.h"
#include <string.h>

/**
 * _strpbrk - Entry point
 * @haystack: pointer
 * @needle: accept
 * Return: char
*/
char *_strstr(char *haystack, char *needle)
{
return (strstr(haystack, needle));
}
