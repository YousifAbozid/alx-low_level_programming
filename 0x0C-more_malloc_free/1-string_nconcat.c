#include "main.h"
#include <stdlib.h>
#include <string.h>

/**
 * string_nconcat - Entry point
 * @n: int
 * @s1: pointer
 * @s2: pointer
 * Return: void
*/
char *string_nconcat(char *s1, char *s2, unsigned int n)
{
void *ptr = malloc(b);
if (ptr == NULL)
exit(98);

return (ptr);
}
